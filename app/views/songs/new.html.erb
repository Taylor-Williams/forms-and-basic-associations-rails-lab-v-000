<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br />

  <%= f.label :song_artist_name %>
  <%= f.text_field :song_artist_name %><br />

  <%= f.text_field :song_genre_id, list: "genres_autocomplete" %>
  <datalist id="genres_autocomplete">
    <% Genre.all.each do |genre| %>
      <option value="<%= genre.name %>">
    <% end %>
  </datalist>

  <input name="song[song_notes][]">
  <input name="song[song_notes][]">
  <input name="song[song_notes][]">
  <input name="song[song_notes][]">
  <input name="song[song_notes][]">
  <input name="song[song_notes][]">
  <input name="song[song_notes][]">

  <%= f.submit %>
<% end %>
